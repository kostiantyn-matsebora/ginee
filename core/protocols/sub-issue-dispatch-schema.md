---
audience: all-cardinals
load: on-demand
triggers: [sub-issue, sub-issue dispatch, sub-issue tracking, progress comment, closing comment]
cap-bytes: 6144
reads-before-applying: [core/templates/sub-issue-dispatch.md]
---

# Sub-issue dispatch schema — merged into template

Template + lints + worked example + forbidden patterns + self-lint checks now consolidated into **`core/templates/sub-issue-dispatch.md`** (single canonical file per artefact).

Existing cites to this schema file resolve to the template — content unchanged in meaning.

<!-- self-lint: pass -->
